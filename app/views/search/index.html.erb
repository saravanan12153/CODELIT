<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-content">
          <h2>
            <%= @courses.count %> results found for: <span class="text-navy">“<%= params[:q][:title_or_description_cont] %>”</span>
          </h2>

          <!--<small>Request time  (0.23 seconds)</small>-->

          <!--<div class="search-form">
            <form action="#" method="get">
              <div class="input-group">
                <input type="text" placeholder="Admin Theme" name="search" class="form-control input-lg">
                <div class="input-group-btn">
                  <button class="btn btn-lg btn-primary" type="submit">
                    Search
                  </button>
                </div>
              </div>

            </form>
          </div>-->
          <% @courses.each do |course| %>
              <div class="hr-line-dashed"></div>
              <div class="search-result">
                <h3><a href="<%= course_url(course) %>"><%= course.title %></a></h3>
                <a href="<%= course_url(course) %>" class="search-link"><%= course_url(course) %></a>

                <p>
                  <% require 'rubygems'
                     require 'nokogiri' %>
                  <%= Nokogiri::HTML(course.description).text.truncate(200, separator: ' ') %>
                </p>
              </div>
          <% end %>
          <div class="hr-line-dashed"></div>
        </div>
      </div>
    </div>
  </div>
</div>
